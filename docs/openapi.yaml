openapi: 3.1.0
info:
  title: TASK API
  version: 1.0.0
servers:
  - url: http://localhost:3000
    description: local host
  - url: https://task-api-vmbs.onrender.com
    description: Production server
tags:
  - name: Tasks
    description: Task related endpoints
paths:
  /tasks:
    $ref: paths/tasks.yaml
  /tasks/{id}:
    $ref: ./paths/taskById.yaml
